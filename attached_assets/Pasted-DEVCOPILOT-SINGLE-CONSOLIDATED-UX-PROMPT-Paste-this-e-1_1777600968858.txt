DEVCOPILOT — SINGLE CONSOLIDATED UX PROMPT
==========================================
Paste this entire prompt into Replit AI in one go after the backend
validation gate passes. It only touches /client/src/ — zero server changes.
==========================================

Rebuild the entire DevCopilot frontend in /client/src/ with a best-in-class
developer tool UX. The backend is complete. Do not modify any file in /server/.
Do not change any API endpoint URLs or request/response shapes. Only replace
and upgrade the visual and interaction layer.

Install react-syntax-highlighter as the only permitted new dependency.
Every other requirement must be met with React, TailwindCSS, inline styles,
CSS variables, and SVG drawn inline. No icon libraries. No animation libraries.
No UI component libraries.

════════════════════════════════════════════════════════════════
STEP 1 — DESIGN TOKENS
════════════════════════════════════════════════════════════════

Create /client/src/styles/tokens.css and import it at the top of
/client/src/main.tsx before anything else:

:root {
  --bg-app:         #0D0F12;
  --bg-surface:     #161A1F;
  --bg-raised:      #1E2329;
  --border:         #2A2F38;
  --border-bright:  #3D4450;
  --text-primary:   #E8EAF0;
  --text-secondary: #8B92A5;
  --text-muted:     #555E70;
  --accent-blue:    #4D9CFF;
  --accent-purple:  #8B7CF8;
  --accent-green:   #3DD68C;
  --accent-amber:   #F5A623;
  --accent-red:     #F06565;
  --radius-sm:      6px;
  --radius-md:      10px;
  --radius-lg:      16px;
  --font-sans:      'Inter', sans-serif;
  --font-mono:      'JetBrains Mono', monospace;
}

Add to index.html inside <head>:
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

In index.css set:
  body { background: var(--bg-app); color: var(--text-primary); }

Custom scrollbar styles in index.css:
  ::-webkit-scrollbar { width: 4px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--border-bright); border-radius: 4px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--text-muted); }

════════════════════════════════════════════════════════════════
STEP 2 — REUSABLE UI COMPONENT LIBRARY
════════════════════════════════════════════════════════════════

Create /client/src/components/ui/ with the following files.
Use only CSS custom properties from tokens.css for all colours.
No Tailwind inside these components — use inline styles only.

── Badge.tsx ──────────────────────────────────────────────────
Props: label: string, variant: 'purple'|'blue'|'green'|'red'|'amber'|'muted'
A small pill. Background is the accent colour at 15% opacity. Text is the
accent colour at full opacity. Font 11px, padding 2px 8px,
border-radius var(--radius-sm), font-weight 500.

── StackBadge.tsx ─────────────────────────────────────────────
Props: stackProfile: StackProfile (imported from /shared/types/stack.ts)
Renders three Badge pills side by side with an 4px gap:
  - Frontend pill: variant blue, label = stackProfile.frontend (e.g. "Angular")
  - Backend pill: variant purple, label = stackProfile.backend (e.g. ".NET")
  - Database pill: variant 'muted' with teal text, label = stackProfile.database
If any field is missing or 'none', show "Unknown" in muted style.

── Button.tsx ─────────────────────────────────────────────────
Props: label: string, onClick: function, variant: 'primary'|'outline'|'ghost'|'danger',
       size: 'sm'|'md', disabled?: boolean, loading?: boolean, icon?: ReactNode
Three visual states managed internally with useState:
  default  — normal appearance
  loading  — spinner SVG replaces the icon (or appears left of label if no icon).
             Spinner: 14px circle, stroke var(--accent-blue), stroke-dasharray 60 20,
             rotating 0.8s linear infinite. Label text unchanged.
  success  — green checkmark SVG appears for exactly 2 seconds then resets to default.
             Trigger success state by calling an exposed triggerSuccess() ref method.
Variant styles (inline):
  primary: background var(--accent-blue), color #0D0F12, border none
  outline: background transparent, border 1px solid var(--border-bright), color var(--text-primary)
  ghost:   background transparent, border none, color var(--text-secondary)
  danger:  background var(--accent-red) at 15% opacity, color var(--accent-red), border 1px solid var(--accent-red)
Size styles: sm = padding 6px 12px font 12px, md = padding 8px 16px font 13px
All buttons: border-radius var(--radius-md), font-weight 500, cursor pointer
Transitions: background 150ms ease, transform 80ms ease
Active press: transform scale(0.97)
Disabled: opacity 0.4, cursor not-allowed
Focus ring: outline 2px solid var(--accent-blue), outline-offset 2px

── Skeleton.tsx ───────────────────────────────────────────────
Props: width?: string|number, height?: string|number, borderRadius?: string
An animated pulsing block. Use @keyframes pulse defined in a <style> tag
inside the component that alternates background between var(--bg-raised)
and var(--border) over 1.4s ease-in-out infinite. Default height 16px,
width 100%, border-radius var(--radius-sm).

── Toast.tsx + useToast.ts ────────────────────────────────────
A fixed bottom-right toast container (position fixed, bottom 24px, right 24px,
z-index 9999, display flex flex-column gap 8px).

Toast types:
  success — 4px left border var(--accent-green), auto-dismisses after 4 seconds
  error   — 4px left border var(--accent-red), persists until the user clicks it

Each toast card: background var(--bg-surface), border 1px solid var(--border),
border-radius var(--radius-md), padding 12px 16px, max-width 320px,
font 13px var(--text-primary). Slide in from right on appear.

useToast hook returns: { showSuccess(message: string): void, showError(message: string): void }
Toasts stack — multiple can be visible simultaneously.

── Tooltip.tsx ────────────────────────────────────────────────
Props: content: string, children: ReactNode
On hover shows a dark tooltip above the child element.
Tooltip: background var(--bg-raised), border 1px solid var(--border),
color var(--text-primary), font 12px, padding 4px 8px,
border-radius var(--radius-sm). Pure CSS — no JS positioning.
Use position absolute, bottom calc(100% + 6px), left 50%, transform translateX(-50%).
Wrap children in a position:relative inline-flex container.

── index.ts ───────────────────────────────────────────────────
Export all components: Badge, StackBadge, Button, Skeleton, Toast, useToast, Tooltip

════════════════════════════════════════════════════════════════
STEP 3 — APP SHELL AND SIDEBAR
════════════════════════════════════════════════════════════════

Create /client/src/context/RepoContext.tsx
A React context storing:
  activeRepository: Repository | null
  setActiveRepository: (repo: Repository | null) => void
  stackProfile: StackProfile | null
Export RepoContext and a useRepo() hook.
Wrap the entire app in RepoContext.Provider in App.tsx.

Create /client/src/components/layout/Sidebar.tsx
Props: isAzureConnected: boolean, isJiraConnected: boolean

Layout: position fixed, left 0, top 0, width 220px, height 100vh,
background var(--bg-surface), border-right 1px solid var(--border),
display flex, flex-direction column, z-index 100.
At viewport < 1200px: collapse to 48px wide showing icons only (use CSS media query).

Top section — wordmark (padding 20px 16px):
  A small purple hexagon SVG (20px, drawn inline, filled var(--accent-purple))
  to the left of the text "DevCopilot" in white 15px bold.
  At collapsed width: show only the hexagon SVG, centred.

Nav section (flex 1, padding 8px):
  Three nav items: Tasks, History, Settings
  Each item: display flex, align-items center, gap 10px, padding 10px 12px,
  border-radius var(--radius-md), cursor pointer, font 13px,
  transition background 150ms ease.
  Active item: color var(--accent-blue), background var(--bg-raised),
  border-left 3px solid var(--accent-blue).
  Inactive: color var(--text-muted), hover background var(--bg-raised).
  Each icon is a 16×16 inline SVG:
    Tasks    — inbox tray outline
    History  — clock outline
    Settings — gear outline
  At collapsed width: show icons only, centred. Wrap each in <Tooltip> showing the label.

Stack info section (padding 12px 16px, border-top 1px solid var(--border)):
  Label "ACTIVE REPO" in 10px var(--text-muted) letter-spacing 0.08em uppercase.
  Repo name in 12px var(--font-mono) var(--text-primary) truncated.
  <StackBadge stackProfile={stackProfile} /> from RepoContext.
  A "Change repo" text link in 11px var(--accent-blue).
  Hide entirely at collapsed width.

Bottom section (padding 16px):
  Two status pills stacked with 4px gap:
    Each pill: display flex, align-items center, gap 6px, font 12px.
    A 6px circle dot: green (#3DD68C) if connected, red (#F06565) if not.
    Text: "Azure DevOps" / "JIRA"
  At collapsed width: show only the dots, centred, stacked.
  Avatar circle below: 32px diameter, background var(--accent-purple) at 20% opacity,
  color var(--accent-purple), font 13px bold, text "DC", border-radius 50%.

Create /client/src/components/layout/AppShell.tsx
display flex, flex-direction row, min-height 100vh.
Sidebar on left. Main content: flex 1, margin-left 220px (48px at < 1200px),
background var(--bg-app), overflow-y auto. Transition margin-left 200ms ease.

Update App.tsx:
  Wrap all routes in <AppShell>.
  Pass isAzureConnected and isJiraConnected as true for now (wire to real state later).
  Keep all existing React Router routes unchanged.

════════════════════════════════════════════════════════════════
STEP 4 — TASKS PAGE (layout + filters + task rows)
════════════════════════════════════════════════════════════════

Rebuild /client/src/pages/TasksPage.tsx entirely.

── Header row ─────────────────────────────────────────────────
Left: "Sprint backlog" in 18px semi-bold var(--text-primary).
Right: task count badge ("N tasks" in muted style, using <Badge variant="muted">)
       + <Button variant="outline" size="sm" label="Sync"> with a rotating
       refresh SVG icon that spins (animation: spin 1s linear infinite) while
       the fetch is in flight.

── Repository bar (below header) ──────────────────────────────
A horizontal row: background var(--bg-surface), border 1px solid var(--border),
border-radius var(--radius-md), padding 10px 16px, margin-bottom 16px.
Left: a dropdown <select> listing connected repos (populated from GET /api/repositories).
      Selected repo name shown with its StackBadge inline to the right of the select.
      On change: call setActiveRepository() from RepoContext and call redetectStack().
Middle: a small <Button variant="ghost" size="sm" label="Connect repo"> that opens
        a modal (see below).
Right: a <Button variant="ghost" size="sm" label="Re-detect stack"> that calls
       GET /api/repositories/:id/stack and updates the RepoContext stackProfile.
       Show a success toast "Stack detected: {frontend} · {backend} · {db}" on success.

Connect repo modal: a fixed overlay (background rgba(0,0,0,0.6)) containing a centred
card (background var(--bg-surface), border var(--border), border-radius var(--radius-lg),
padding 24px, width 480px). Fields:
  - Repository URL (text input)
  - Provider (select: GitHub / Azure Repos)
  - Default branch (text input, default "main")
  Submit calls POST /api/repositories. On success: show detected StackBadge in the modal,
  close after 2 seconds, update the repo dropdown.
  Close button (×) top-right. Escape key closes it.

── Filter bar ─────────────────────────────────────────────────
Create /client/src/components/tasks/FilterPills.tsx
A horizontal row of pill toggle buttons, gap 6px, flex-wrap wrap.
At < 768px: overflow-x auto, no-wrap.
Three filter groups separated by a 1px vertical divider (var(--border)):
  Type:     All · Epic · Story · Task · Bug
  Source:   All · Azure DevOps · JIRA
  Priority: All · P1 · P2 · P3 · P4
Pill active state: background var(--accent-blue) at 20% opacity,
color var(--accent-blue), border 1px solid var(--accent-blue).
Pill inactive: background transparent, color var(--text-muted),
border 1px solid var(--border), hover border var(--border-bright).
Pill style: padding 4px 12px, border-radius 20px, font 12px 500, cursor pointer,
transition all 150ms ease.
All filtering is pure client-side on the fetched tasks array. No API calls on filter change.

── Task list states ────────────────────────────────────────────
Loading: render 6 <Skeleton height={64} borderRadius="var(--radius-md)" /> rows,
         gap 8px between them.
Empty:   centred column — a simple inbox SVG illustration (~80px, drawn inline,
         stroked var(--text-muted)), "No tasks found" in 14px var(--text-primary),
         "Sync your board to load tasks" in 13px var(--text-muted).
Error:   a banner with border-left 4px solid var(--accent-red),
         background var(--accent-red) at 10% opacity, padding 12px 16px,
         border-radius var(--radius-md), the error message in var(--accent-red),
         and a <Button variant="outline" size="sm" label="Retry"> on the right.
Success: render the filtered task list using <TaskRow> components.

── Task row component ──────────────────────────────────────────
Create /client/src/components/tasks/TaskRow.tsx
Props: task: DevCopilotTask, onGenerate: () => void

Outer card: background var(--bg-surface), border 1px solid var(--border),
border-radius var(--radius-md), display flex, overflow hidden,
transition background 150ms ease, border 150ms ease, cursor pointer.
Hover: background var(--bg-raised), border-color var(--border-bright).
Clicking the card (not the Generate button) navigates to /workspace/:task.id.

Far left: a 3px wide full-height vertical bar (no border-radius on right side):
  Epic → var(--accent-purple)
  Story → var(--accent-blue)
  Task → var(--accent-green)
  Bug → var(--accent-red)

Content area (flex 1, flex-direction column, padding 12px 16px, gap 4px):
  Row 1: Task ID in 11px var(--font-mono) var(--text-muted) — flex spacer —
         <Badge> for source (purple=azure-devops, blue=jira) —
         <Badge> for type — <Badge> for priority (amber P1/P2, muted P3/P4).
  Row 2: Task title in 14px semi-bold var(--text-primary).
  Row 3: Description truncated to 1 line (overflow hidden, text-overflow ellipsis,
         white-space nowrap), 13px var(--text-secondary).
  At < 768px: hide Row 3.

Far right (display flex, align-items center, gap 8px, padding-right 16px):
  Assignee circle: 28px diameter, background var(--bg-raised), border-radius 50%,
  font 11px var(--text-muted), showing first letter of assignee name or "?" if null.
  <Button variant="primary" size="sm" label="Generate"
          onClick={(e) => { e.stopPropagation(); onGenerate(); }}>

── Data fetching and wiring ────────────────────────────────────
Create /client/src/services/api.ts with fully typed functions:

  fetchTasks(): Promise<DevCopilotTask[]>
    GET /api/tasks

  fetchRepositories(): Promise<Repository[]>
    GET /api/repositories

  connectRepository(data: { name: string; provider: string; url: string; defaultBranch: string }): Promise<Repository>
    POST /api/repositories

  redetectStack(repoId: string): Promise<StackProfile>
    GET /api/repositories/:repoId/stack

  generateSuggestions(taskId: string, refinementPrompt?: string): Promise<CodeSuggestion[]>
    POST /api/tasks/:taskId/suggestions with body { refinementPrompt }

  commitCode(taskId: string, filePath: string, code: string, commitMessage: string): Promise<{ commitHash: string; prUrl: string }>
    POST /api/tasks/:taskId/commit

  completeTask(taskId: string, commitHash: string): Promise<{ success: boolean }>
    POST /api/tasks/:taskId/complete

All functions must throw ApiError { message: string; status: number } on non-2xx.

In TasksPage wire:
  - fetchTasks() on mount and on Sync button click. Show skeleton while loading.
  - fetchRepositories() on mount. Populate the repo dropdown.
  - On repo select: update RepoContext.activeRepository, show StackBadge.
  - On Connect repo submit: call connectRepository(), show detected stack on success.
  - Filter the fetched task array client-side whenever filter pills change.
  - Update the task count badge with the filtered count.
  - On Generate button click: navigate to /workspace/:taskId.
  - Use useToast: "Tasks synced" on sync success, error message on failure.

Page title: document.title = "DevCopilot — Sprint Backlog" (in a useEffect).

════════════════════════════════════════════════════════════════
STEP 5 — WORKSPACE PAGE (all three panels)
════════════════════════════════════════════════════════════════

Rebuild /client/src/pages/WorkspacePage.tsx entirely.

Overall layout: display flex, flex-direction row, height 100vh (or calc(100vh)),
overflow hidden. No page-level scroll. Each column scrolls independently.

Shared state managed in WorkspacePage with useState:
  task: DevCopilotTask | null
  suggestions: CodeSuggestion[]
  acceptedSuggestion: CodeSuggestion | null
  commitHash: string | null
  prUrl: string | null
  currentStep: 1 | 2 | 3 | 4   (starts at 1)
  isComplete: boolean
  refinementPrompt: string
  confirmDialogOpen: boolean
  shortcutsOpen: boolean
  isGenerating: boolean
  isCommitting: boolean
  isCompleting: boolean

On mount:
  1. Read taskId from useParams().
  2. Fetch task details from the tasks array already in memory (or call GET /api/tasks
     and filter). Show skeleton in the left panel if loading.
  3. Immediately call generateSuggestions(taskId) and set isGenerating = true.
     On success: set suggestions, set currentStep = 1, show toast "Suggestions generated".
     On error: show error toast "Failed to generate — {message}".

Keyboard shortcuts (attach to window in a useEffect, clean up on unmount):
  R         → call generateSuggestions() again (same as Re-generate button)
  Ctrl/Cmd+Enter → trigger commit if acceptedSuggestion !== null and !isCommitting
  Escape    → if confirmDialogOpen set it false, else if shortcutsOpen set it false
  ?         → toggle shortcutsOpen

Shortcuts panel: when shortcutsOpen is true, render a fixed bottom-left card
(background var(--bg-surface), border var(--border), border-radius var(--radius-lg),
padding 16px, width 260px, z-index 200) listing all shortcuts as two-column rows
(key pill left, description right). Clicking outside or pressing Escape closes it.

── LEFT PANEL ─────────────────────────────────────────────────
Width 280px, flex-shrink 0, background var(--bg-surface),
border-right 1px solid var(--border), overflow-y auto, padding 20px.
At < 1200px: width 100%, max-height 400px.
At < 768px: full-width panel inside a tab system (see responsive section).

Back link (margin-bottom 16px):
  display flex, align-items center, gap 6px. Left-arrow SVG (14px, var(--text-muted))
  + "Sprint backlog" in 13px var(--text-muted). Clicking navigates to /tasks.

Task header (margin-top 16px):
  <Badge> source + <Badge> type on one line, gap 6px.
  Task title: 16px semi-bold var(--text-primary), margin-top 8px.
  Priority row: a 8px circle dot (var(--accent-amber) for P1/P2, var(--text-muted)
  for P3/P4) + "Priority {task.priority}" in 13px var(--text-secondary).

Divider: 1px solid var(--border), margin 16px 0.

Repository stack section:
  Label "REPOSITORY STACK" — 10px var(--text-muted) letter-spacing 0.08em uppercase.
  <StackBadge stackProfile={stackProfile} /> from useRepo().
  Note below: "AI suggestions will target this stack" — 11px var(--text-muted) italic.

Divider.

Description section:
  Label "DESCRIPTION" — same muted caps style as above.
  Description text: 13px var(--text-secondary), line-height 1.6.

Divider.

Acceptance criteria section:
  Label "ACCEPTANCE CRITERIA" — muted caps.
  Each criterion: display flex, align-items flex-start, gap 8px, padding 6px 0,
  cursor pointer. Clicking toggles local checked state per criterion (useState array).
  Unchecked: a 16px circle outline (var(--border-bright)).
  Checked: a 16px green checkmark SVG (var(--accent-green)) + text color
  var(--accent-green) + text-decoration line-through.

Divider.

Refine with AI section:
  Label "REFINE WITH AI" — muted caps.
  Textarea: rows 4, width 100%, background var(--bg-raised), border 1px solid var(--border),
  border-radius var(--radius-md), color var(--text-primary), font 13px var(--font-sans),
  padding 10px, resize vertical. Focus: border-color var(--accent-blue), outline none.
  Placeholder: "Ask the agents to adjust the approach..."
  Bound to refinementPrompt state.

  Quick-prompt chips (flex-wrap wrap, gap 6px, margin-top 8px):
    "Add error handling" / "Add unit tests" / "Optimise performance" / "Add TypeScript types"
    Each chip: padding 4px 10px, border 1px solid var(--border), border-radius 20px,
    font 12px var(--text-muted), cursor pointer. Hover: border var(--accent-blue),
    color var(--accent-blue). Clicking appends chip text to the textarea.

  Re-generate button (full width, margin-top 12px):
    <Button variant="primary" size="md" label="Re-generate"
            icon={<SparkSVG />} loading={isGenerating}
            onClick={() => generateSuggestions(taskId, refinementPrompt)}>
    SparkSVG: a 4-pointed star drawn inline, 14×14px.

── CENTRE PANEL ───────────────────────────────────────────────
flex 1, display flex, flex-direction column, background var(--bg-app),
overflow hidden. At < 1200px: width 100%, max-height 400px.

TOP ZONE — 60% height — Agent tabs:

Tab bar (flex-shrink 0):
  display flex, background var(--bg-surface), border-bottom 1px solid var(--border),
  padding 0 16px, overflow-x auto.

  Four agent tabs: Claude · OpenAI · Anti Gravity · MS Copilot.
  On load, sort tabs so the recommended agent (highest score) appears first.

  Each tab button: padding 12px 16px, font 13px semi-bold, cursor pointer,
  border: none, background transparent, border-bottom 2px solid transparent,
  display flex, align-items center, gap 8px, flex-shrink 0.
  Active tab: color var(--text-primary), border-bottom-color in agent colour.
  Inactive: color var(--text-muted). Hover: color var(--text-secondary).
  Agent accent colours:
    Claude      → var(--accent-purple)
    OpenAI      → var(--accent-green)
    Anti Gravity→ var(--accent-amber)
    MS Copilot  → var(--accent-blue)

  Inside each tab button:
    Agent name text.
    Score badge: if score exists, a small pill showing e.g. "8.4" in accent colour.
    "Best" pill (green, font 10px) on the top-scoring tab only.

Loading state per tab:
  While isGenerating, show three pulsing dots in the agent accent colour
  (8px circles, animation stagger 0s / 0.2s / 0.4s, scale 1 to 1.4 and back).
  Text "Generating..." in 12px var(--text-muted).
  Tabs reveal progressively as each Promise.allSettled() result arrives,
  with 200ms stagger between reveals.

Tab content area (flex 1, overflow-y auto):

  Explanation banner:
    Italic 13px var(--text-secondary), background var(--bg-raised),
    padding 10px 16px, border-bottom 1px solid var(--border).

  File path bar:
    display flex, align-items center, justify-content space-between,
    padding 8px 16px, border-bottom 1px solid var(--border).
    File path in 12px var(--font-mono) var(--text-muted).
    Copy icon button (clipboard SVG, 14px): on click copies file path to clipboard,
    shows <Tooltip content="Copied!"> for 2 seconds via local state.

  Code block:
    Use <SyntaxHighlighter> with a custom style matching the design system:
      background: "#0D0F12", fontSize: "13px",
      fontFamily: "var(--font-mono)", lineHeight: "1.6",
      padding: "16px", margin: 0
    showLineNumbers: true.
    Position the code block relatively. Add a copy button (clipboard SVG, 14px)
    absolutely top-right (top 8px, right 8px) with the same "Copied!" tooltip.
    Language detection from filePath extension:
      .ts / .tsx → "typescript"
      .cs        → "csharp"
      .java      → "java"
      .py        → "python"
      .html      → "html"
      .sql       → "sql"
      anything else → "typescript" as default

  Score bars (padding 16px, border-top 1px solid var(--border)):
    Four bars: Correctness · Readability · Minimal diff · Convention adherence.
    Each bar: display flex, align-items center, gap 8px, margin-bottom 8px.
      Label: 12px var(--text-secondary), width 140px.
      Track: flex 1, height 4px, background var(--bg-raised), border-radius 2px.
      Fill: width = (score * 10)%, background var(--accent-blue), border-radius 2px.
      Percentage: 12px var(--text-muted), width 36px, text-align right.
    If score is not yet available, show all bars at 0% with skeleton appearance.

  Accept button (padding 16px, border-top 1px solid var(--border)):
    <Button variant="primary" size="md" label="Accept this suggestion"
            onClick={() => { setAcceptedSuggestion(suggestion); setCurrentStep(2); }}>
    Full width (width 100%).

BOTTOM ZONE — 40% height — Diff view:

  Header bar: display flex, align-items center, gap 8px, padding 10px 16px,
  background var(--bg-surface), border-top 1px solid var(--border),
  border-bottom 1px solid var(--border).
    "Changes" in 12px semi-bold var(--text-primary).
    "+14" in var(--accent-green) + " -3" in var(--accent-red) in a small badge.

  If no accepted suggestion yet: centred message "Select a suggestion to see the diff"
  in 13px var(--text-muted).
  If the file is new: centred message "New file — no diff available".
  Otherwise: split view — two equal columns, each with a header ("Current" / "Suggested"
  in 11px var(--text-muted) uppercase) and the relevant code content.
    Added lines: background var(--accent-green) at 8% opacity,
    border-left 3px solid var(--accent-green), padding-left 4px.
    Removed lines: background var(--accent-red) at 8% opacity,
    border-left 3px solid var(--accent-red), padding-left 4px.
  Use the same mono font and dark background as the code block above.

── RIGHT PANEL ────────────────────────────────────────────────
Width 280px, flex-shrink 0, background var(--bg-surface),
border-left 1px solid var(--border), overflow-y auto, padding 20px.
At < 1200px: width 100%, max-height 400px.

Create /client/src/components/workspace/Stepper.tsx
Props: currentStep: 1 | 2 | 3 | 4
Four steps rendered as a vertical list, each step: display flex, align-items flex-start,
gap 12px, position relative.

Step circle (28px, border-radius 50%):
  Completed (step < currentStep): background var(--accent-green), white checkmark SVG inside (12px).
  Current   (step === currentStep): background var(--accent-blue), CSS pulse animation
    (keyframes: scale 1 → 1.15 → 1, 1.5s ease-in-out infinite).
  Pending   (step > currentStep): background var(--bg-raised), border 1px solid var(--border).

Connector line between steps: position absolute, left 13px, top 28px, width 2px,
height calc(100% + 8px). Filled var(--accent-green) for completed connections,
var(--border) for pending. Animate height fill with transition height 400ms ease.

Step label: 13px var(--text-primary) for current/completed, var(--text-muted) for pending.

Step labels: 1 "Suggestions ready" / 2 "Code accepted" / 3 "PR opened" / 4 "Task closed"

Divider (margin 16px 0).

Accepted suggestion card:
  Label "ACCEPTED SUGGESTION" — 10px var(--text-muted) letter-spacing 0.08em uppercase.
  If nothing accepted:
    A dashed-border card (border 1px dashed var(--border), border-radius var(--radius-md),
    padding 12px, text-align center, color var(--text-muted) 13px):
    "No suggestion accepted yet"
  If accepted:
    background var(--bg-raised), border 1px solid var(--border),
    border-radius var(--radius-md), padding 12px.
    Agent name in agent accent colour (13px bold).
    Score badge (e.g. "8.4 / 10") in var(--text-secondary) 12px.
    File path in 12px var(--font-mono) var(--text-muted), truncated.

Divider.

Commit section:
  Label "COMMIT DETAILS" — muted caps.

  Branch name input:
    Full width, font var(--font-mono) 12px, background var(--bg-raised),
    border 1px solid var(--border), border-radius var(--radius-md),
    padding 8px 12px, color var(--text-primary), cursor default.
    Value: "task/{taskId}". readOnly.

  Commit message input (margin-top 8px):
    Same styles but cursor text, editable. Pre-filled with task.title.
    On change: update local state.

  Commit button (margin-top 12px):
    <Button variant="primary" size="md" label="Commit + open PR"
            loading={isCommitting}
            disabled={!acceptedSuggestion || isCommitting}>
    Full width. On click:
      1. Set isCommitting = true.
      2. Call commitCode(taskId, acceptedSuggestion.filePath,
                         acceptedSuggestion.code, commitMessage).
      3. On success: set commitHash, set prUrl, set currentStep = 3,
         show toast "PR opened successfully", trigger button success state.
      4. On error: show error toast "Commit failed — {message}".
      5. Set isCommitting = false.
    After success, render the PR URL as a clickable <a> link below the button
    in var(--accent-blue) 12px.

Divider.

Complete task section:
  Label "COMPLETE TASK" — muted caps.

  If !isComplete, show:
    <Button variant="primary" size="md" label="Mark task complete"
            loading={isCompleting}
            disabled={!prUrl || isCompleting}>
    Style override: background var(--accent-green), color #0D0F12.
    Full width.
    On click: set confirmDialogOpen = true.

  Inline confirmation card (appears below the button when confirmDialogOpen):
    background var(--bg-raised), border 1px solid var(--border-bright),
    border-radius var(--radius-md), padding 14px, margin-top 8px.
    "Close this task in {task.source}?" in 13px var(--text-primary).
    Two buttons side by side (gap 8px, margin-top 10px):
      <Button variant="primary" size="sm" label="Confirm" loading={isCompleting}
              onClick={handleComplete}>  (override background to var(--accent-green))
      <Button variant="outline" size="sm" label="Cancel"
              onClick={() => setConfirmDialogOpen(false)}>

  handleComplete():
    1. Set isCompleting = true.
    2. Call completeTask(taskId, commitHash).
    3. On success: set isComplete = true, set currentStep = 4,
       set confirmDialogOpen = false,
       show toast "Task closed in {task.source}".
    4. On error: show error toast "Could not close task — {message}".
    5. Set isCompleting = false.

Success state (replaces entire right panel content when isComplete = true):
  display flex, flex-direction column, align-items center, justify-content center,
  height 100%, text-align center, padding 20px.

  SVG checkmark circle drawn with stroke animation:
    Circle: cx 32 cy 32 r 28, stroke var(--accent-green), stroke-width 3, fill none.
    stroke-dasharray 176, stroke-dashoffset 176.
    @keyframes drawCircle: stroke-dashoffset 176 → 0 over 0.5s ease.
    Checkmark path (M18 32 L28 42 L46 22), same stroke, draws 0.3s after circle
    (animation-delay 0.5s).
    SVG: 64×64px.

  "Task closed" text: 16px semi-bold var(--accent-green), margin-top 16px.
  Task ID: 12px var(--font-mono) var(--text-muted), margin-top 4px.
  <Button variant="outline" size="md" label="Back to backlog"
          onClick={() => navigate('/tasks')}> margin-top 20px.

════════════════════════════════════════════════════════════════
STEP 6 — RESPONSIVE BEHAVIOUR
════════════════════════════════════════════════════════════════

Use CSS media queries only — no JS resize libraries.

At viewport width < 1200px:
  Sidebar: collapse to 48px, show icons only (text and stack info hidden).
  Workspace: flex-direction column instead of row.
  Each panel: width 100%, max-height 400px, overflow-y auto.

At viewport width < 768px:
  Show a dismissible banner at the top of WorkspacePage:
    background var(--bg-raised), border-bottom 1px solid var(--border),
    border-left 3px solid var(--accent-amber), padding 10px 16px.
    Text: "DevCopilot works best on a wider screen" in 13px var(--text-primary).
    An × button (var(--text-muted)) on the right dismisses it (local state).

  WorkspacePage becomes a single-column tab layout.
    Tab bar (style matches agent tab bar): three tabs: Task · Suggestions · Actions.
    Each tab shows the full corresponding panel. Only one panel visible at a time.

  TasksPage filter bar: overflow-x auto, white-space nowrap on the pill row.
  TaskRow: hide the description row. Show only title and badges.
  Repo bar: show selected repo name + StackBadge as a single truncated line.

════════════════════════════════════════════════════════════════
STEP 7 — FINAL POLISH
════════════════════════════════════════════════════════════════

Apply these improvements across every file that already exists in /client/src/.
Do not add new features — only improve existing UI.

1. TRANSITIONS — add to every interactive element that does not already have one:
   Buttons:      background 150ms ease, transform 80ms ease
   Cards:        background 150ms ease, border-color 150ms ease
   Sidebar items:background 150ms ease
   Tab indicator: border-color 200ms ease
   Stepper line:  height 400ms ease
   Button active: transform scale(0.97) on :active

2. FOCUS STATES — all interactive elements keyboard-navigable:
   outline: 2px solid var(--accent-blue); outline-offset: 2px
   Never remove outline without providing this replacement.

3. STATE COMPLETENESS — audit every data area:
   Repo list dropdown: loading state (disabled + "Loading..."), empty state, error.
   Stack detection result: skeleton while detecting, error toast on failure.
   Task list: loading (skeletons), empty, error — all three required.
   Suggestions: loading (pulsing dots), empty/error state per tab.
   All states must be visually distinct and not rely on colour alone.

4. TYPOGRAPHY — enforce across all components:
   Section labels: font-size 10px, color var(--text-muted),
     letter-spacing 0.08em, text-transform uppercase, font-weight 500.
   Body text: 13px var(--text-secondary), line-height 1.6.
   Titles and headings: 14px–18px var(--text-primary), font-weight 600.
   Mono text (IDs, file paths, branch names, stack tech names):
     font-family var(--font-mono), font-size 12px, color var(--text-muted).
   Fix any element that deviates from these rules.

5. PAGE TITLES — use document.title in a useEffect in each page:
   TasksPage:     "DevCopilot — Sprint Backlog"
   WorkspacePage: "DevCopilot — {task.title} [{stack.frontend} · {stack.backend}]"
                  e.g. "DevCopilot — Fix login bug [Angular · .NET]"
                  While task is loading: "DevCopilot — Loading..."

6. TOAST COMPLETENESS — ensure useToast is called for every async action:
   fetchTasks success:        "Tasks synced"
   fetchTasks error:          "Sync failed — {message}"
   connectRepository success: "Repository connected — stack detected"
   connectRepository error:   "Failed to connect repository — {message}"
   redetectStack success:     "Stack detected: {frontend} · {backend} · {db}"
   redetectStack error:       "Stack detection failed — {message}"
   generateSuggestions success:"Suggestions generated"
   generateSuggestions error:  "Failed to generate — {message}"
   code copied to clipboard:   "Code copied"
   commitCode success:         "PR opened successfully"
   commitCode error:           "Commit failed — {message}"
   completeTask success:       "Task closed in {source}"
   completeTask error:         "Could not close task — {message}"

7. CONSOLE AUDIT — open browser DevTools and fix every error and warning.
   Do not suppress with try/catch or console.error overrides.
   Fix the root cause of each one.

════════════════════════════════════════════════════════════════
FINAL CONSTRAINTS — READ BEFORE WRITING ANY CODE
════════════════════════════════════════════════════════════════

- Do NOT modify any file in /server/ or /shared/types/ (read-only for this prompt)
- Do NOT add any npm package except react-syntax-highlighter
- Do NOT use any icon library (Heroicons, Lucide, FontAwesome etc.)
- Do NOT use any animation library (Framer Motion, GSAP etc.)
- Do NOT use any UI component library (shadcn, Radix, MUI etc.)
- All SVG icons must be drawn inline in the component JSX
- All colours must use CSS custom properties from tokens.css — no hardcoded hex values
  except inside tokens.css itself and the SyntaxHighlighter customStyle object
- Every component must be fully typed with TypeScript interfaces
- Apply React.memo to TaskRow for performance
- The full end-to-end flow must work after this prompt:
    load tasks → filter → click Generate → workspace loads → suggestions appear →
    accept a suggestion → commit → PR URL shown → mark complete → success animation →
    back to backlog
- Zero console errors or warnings in the final output